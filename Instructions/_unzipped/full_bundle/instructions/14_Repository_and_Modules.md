# 14 — Repository & Modules
```
/src
  /core      (ecs, math, types, rng)
  /systems   (drive, item, ai, physics, vfx, audio, ui)
  /content   (tracks, items, weights.json, dda.json)
  /scenes    (menu, garage, race)
  index.ts
/assets
  /models    (characters, karts, props)
  /tracks    (Sunny, Jungle, City)
  /textures  (ktx2)
  /audio
/instructions (this folder)
```
